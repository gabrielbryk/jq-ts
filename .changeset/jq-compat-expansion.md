---
'@gabrielbryk/jq-ts': minor
---

Expand jq compatibility for common pure builtins and arities, including `map_values`, `in`, `del`, `pick`, `tojson`, `fromjson`, trim helpers, type filters, `paths(filter)`, `add(generator)`, additional `all`/`any`/`first`/`last`/`nth`/`recurse` forms, jq comments, and destructuring bindings.
